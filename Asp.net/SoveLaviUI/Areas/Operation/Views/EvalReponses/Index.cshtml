@model IEnumerable<BOL.tbl_EVAL_REPONSE>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.commentaires)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_NIVEAU_RESOLUTION.nomNiveau)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_REPONSE.description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.commentaires)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_NIVEAU_RESOLUTION.nomNiveau)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_REPONSE.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
