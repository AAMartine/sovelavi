//script for date in the VULNERABILITE create view
        $(function () { // will trigger when the document is ready
            $('.datepicker').datepicker({
                format: "mm/dd/yyyy",
                autoclose: true
            }); //Initialise any date pickers

        });


